# Exact Git Commands for CI Execution
# Copy-paste ready for immediate execution

# =============================================================================
# COMMAND 1: Verify Remotes Are Configured
# =============================================================================
git remote -v

# Expected output (all four lines must appear):
# fork      https://github.com/xyz99002/rustdesk-direct-ip-remote-support.git (fetch)
# fork      https://github.com/xyz99002/rustdesk-direct-ip-remote-support.git (push)
# upstream  https://github.com/rustdesk/rustdesk.git (fetch)
# upstream  https://github.com/rustdesk/rustdesk.git (push)


# =============================================================================
# COMMAND 2: Verify Repository State
# =============================================================================
git status

# Expected output:
# On branch master
# nothing to commit, working tree clean


# =============================================================================
# COMMAND 3: Verify Latest Commit
# =============================================================================
git log -1 --oneline

# Expected output (updates as new commits land; check against actual HEAD):
# bdb601d0f Correct canonical repository references; fix release.yml tag versioning; audit flutter-ci/nightly failures


# =============================================================================
# COMMAND 4: TRIGGER CI - Push to Fork
# =============================================================================
git push fork master

# Expected output (last line most important):
# ...
# remote: GitHub has received your request to run this workflow
#  * [new branch]      master -> master   (or a fast-forward update line on subsequent pushes)


# =============================================================================
# COMMAND 5: MONITOR WORKFLOWS
# =============================================================================
# Open in browser:
# https://github.com/xyz99002/rustdesk-direct-ip-remote-support/actions

# A push to master triggers TWO workflows:
# - "Direct-IP Build" (direct-ip-build.yml) — Direct-IP-scoped Windows + Linux build/test
# - "Full Flutter CI" (flutter-ci.yml) — the full upstream flutter-build.yml matrix
#   (Windows/macOS/Linux/Android/AppImage/Flatpak); see docs/CI_WORKFLOW_AUDIT_2026-09-01.md
#   for known failures in this second workflow.


# =============================================================================
# ONE-LINE EXECUTION (all verification + push)
# =============================================================================
git remote -v && git status && git log -1 --oneline && git push fork master
